<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:i18n="clr-namespace:SpeechPathology.Extensions"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="SpeechPathology.Views.FlashcardsTestView"
             Title="Flashcards">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Text, Mode=OneWay}"
               HorizontalOptions="Center" FontSize="Large" />

            <ffimageloading:CachedImage x:Name="image" Source="{Binding ImageSource}" Aspect="AspectFit"/>

            <Button Text="{i18n:Translate Play}" BackgroundColor="#358841" TextColor="White" WidthRequest="60" HeightRequest="60" HorizontalOptions="Center"
                    BorderRadius="30" Command="{Binding PlaySoundCommand}" CommandParameter="{Binding Sound}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>