<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:i18n="clr-namespace:SpeechPathology.Extensions"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:cv="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
             x:Class="SpeechPathology.Views.FlashcardsTestView"
             Title="{i18n:Translate SpeechSounds}"
             BackgroundColor="White">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal"
                         HorizontalOptions="Center">
                <Label Text="{Binding Source={x:Reference Flashcards}, Path=Position, Converter={StaticResource ZeroToOneBaseConverter}}"
                       FontSize="Large" />
                <Label Text="{i18n:Translate of}"
                       FontSize="Large" />
                <Label Text="{Binding Source={x:Reference Flashcards}, Path=ItemsSource.Count}"
                       FontSize="Large" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>